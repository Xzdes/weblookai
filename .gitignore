# Created by WeblookAI - to keep the repository clean.

# Node.js dependencies
# This directory contains all the installed packages from npm.
# It can be very large and should always be regenerated by running `npm install`.
/node_modules

# Environment variables file
# Although we removed it, it's a very good practice to always ignore .env files
# in case they are added back for testing or other purposes.
.env

# Log files
# If we decide to add logging to files, we don't want to commit them.
*.log

# Operating System generated files
.DS_Store
Thumbs.db